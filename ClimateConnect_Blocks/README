A web frontend tool built using php and curl that was built for the a private firm 
called Climate Connect. The tool was then used to query the databases for
carbon stocks, and show the relevant data.
